name: GitHub Actions Examen DIW

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: 
  push: ['main']
jobs:
  Examen1:
    runs-on: ubuntu-latest
    steps:
      - run: echo " Mi Nombre es ${{ github.actor }}"
      - run: echo " Mi rama es ${{ github.ref }} mi repositorio es ${{ github.repository }}."